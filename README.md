# To Run
With docker
```
docker run -d --name leaflet-picture-poc -p 8080:80 ghcr.io/quocchungthan/leaflet-picture-poc:latest

```
with npm
```
# Pull the repository
# cd to the repository
npm install && npm start
```